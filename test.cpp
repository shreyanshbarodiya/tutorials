#include <iostream>
#include <vector>
#include <stdio.h>
#include <cstring>

using namespace std;

class nd_pair{
	int neighbour;
	int distance;
};

bool storeFind(int * array, int x, int len);

int main(){
//hii
	int n,m,k,u,v, temp;
	int s;
	int l;
	cin >> n >> m >> k;
	
	int *stores = (int *) malloc(k* sizeof(int));
	vector< vector<nd_pair> > adj(n);

	return 0;
}
